# 2026-09-07 首輪修正與驗證

> 歷史結果；最新進度見[第二輪修正與驗證](修正與驗證-2026-09-07-第二輪.md)。

[bug 表](BUGS.md)｜[測試索引](README.md)

## 本次程式修正

| 範圍（相對 ai-wrapper） | 改動 | 對應 |
|---|---|---|
| `apps/trpg-server/src/services/turnService.ts` | 模型確認本回合施法且允許／調整後才進術式流程；面板只覆蓋同一術式的參數，不覆蓋文字指定的其他術式 | B003 |
| `apps/trpg-server/src/services/dmService.ts` | 提供術式名稱與預選資訊；加強前文承接、同義來源、日常免骰、創意保留與未知地點規則 | B002、B004、B005、B007、B008 |
| `apps/trpg-server/src/services/narrationService.ts` | 要求不捏造可導航地址，保留合理社交意圖 | B007、B008 |
| `apps/trpg-server/src/services/worldUpdateService.ts` | 普通探索／聊天不算拒絕劇情，不把澄清算成失敗鉤子 | B001；僅提示詞改善，未證明本場根因 |
| `apps/trpg-web/app/try/components/Playground.tsx` | 一般文字行動不受未選術式阻擋；地圖旅行文字標明「透過地圖」 | B003、B006 |
| `apps/trpg-web/app/try/components/AbilityHintPanel.tsx` | 說明面板只是施法參數，查看與聊天不會自動施法 | B003 |

## 實際檢查

| 檢查 | 結果 | 證據／限制 |
|---|---|---|
| 新增術式預選回歸案例，修正前執行 | 預期 FAIL | 已重現查看告示，以及 clarify／reject，仍被面板術式導入施法 |
| `bun test src/services/turnService.test.ts`（server） | PASS，52 個 | 修正後查看不施法、不扣 MP、板面列四委託；另驗文字術式不受其他預選術式覆蓋 |
| `bun test`（server） | FAIL：1505 pass／2 fail | 共 1507 個；兩個失敗均在 `worldUpdateService.test.ts` |
| 未修改 HEAD 的 server 副本，執行 `worldUpdateService.test.ts` | FAIL：21 pass／2 fail | 同兩個失敗在本次修改前已存在；使用本機依賴與測試用設定，不呼叫外部服務 |
| `bun test`（web） | PASS：131 pass／0 fail | 25 個測試檔；不等同瀏覽器實際點擊驗收 |
| server／web `bun run check-types` | PASS | TypeScript 靜態型別檢查 |
| server／web `bun run lint` | PASS | ESLint 檢查 |
| `git diff --check` | PASS | 修改差異無空白格式錯誤 |
| Harvey 真模型與真人實測 | 進行中 | 第一場已收錄；本輪修正後的結果待回報。Codex 未自行啟動真模型測試 |

既有失敗名稱：`requestWorldUpdate > 模型的提議過了閘門就回疊加完成的清單`、`mergeWorldUpdate > 合法的提議疊加在既有痕跡後面,不覆蓋`。本次未更改期待值或修補無關規則來讓數字變綠。

## 仍待證據

- B006 已依 Harvey 更正：礦坑移動來自點地圖，撤回文字被誤路由推論。現有本機路徑會建立玩家 action entry；「當場完全沒顯示」尚未重現。
- B001 沒有終局全文或原始事件，不知道是否走 `idleTurns >= 20` 作者出口或動態收束；不隨意改門檻。
- B007 尚未新增世界事實的語意驗證；提示詞並不保證阻止所有幻覺。
- B008 缺朋友原句與改寫對照；沒有自動勾成已修正。
