# TRPG bug 追蹤表

[測試索引](README.md)｜[RUN-001](場次/RUN-001-銀蹄酒館/結果與觀察.md)｜[首輪修正與驗證](修正與驗證-2026-09-07.md)｜[第二輪修正與驗證](修正與驗證-2026-09-07-第二輪.md)
｜[擲骰時機判準與對照證據](參考資料/擲骰時機與對照證據.md)｜[B009 線索備援盤點](參考資料/B009-第一章線索備援盤點.md)

> 更新：2026-09-07。實測入口：`/try` 單人頁（Harvey 已確認）；當場執行版本、角色與模型未知。工程修正不等於真人驗收完成。
> 實測狀態：Harvey 正在做真模型與真人實測；本次修正後的結果待回報，不能寫成「沒有人測」。
> 表格持續新增，ID 不重編。下方 **手動驗收清單** 是修正完成的唯一勾選位置，使用 Obsidian 原生 checkbox；表格內的工程進度不自動替你勾選。

| ID | 優先度 | 問題／實際表現 | 預期行為 | 證據與重現方式 | 工程進度／剩餘工作 |
|---|---|---|---|---|---|
| B003 | 高 | 查看告示被當成施法，公告欄受損 | 文字未宣告施法就不執行術式；不扣 MP、不破壞板面 | RUN-001 T02；魔法面板保留預選術式後查看告示 | 已修正面板參數搶先觸發施法；回歸測試 PASS；當場角色未知，真人待驗 |
| B002 | 高 | 公告欄只剩失蹤礦工，主支線未完整呈現；「告示」可能未對應公告欄 | AI 依前文辨識來源；引擎輸出完整四份可見委託 | T02；第二次正常僅為補充回報，未提供 RUN-002 | B003 回歸案例已驗四任務；提示詞補同義語意規則；真模型同義詞複驗待做 |
| B004 | 高 | 普通搭話、詢問公開委託人也擲骰 | 結果不確定且失敗有實質後果才檢定 | T03、T05；重跑相同日常交談 | 已新增 `checkReason` 與引擎防線：普通行動未提供不確定性及失敗後果就不骰；作者線索、攻擊、狀態保留原規則。**再補兩條引擎缺口**（`checkReason` 只在模型「沒填依據」時生效，模型硬填依據仍會擲）：① **公開資訊源降級**：`informationSources` 的 `coverage` 只有 `"complete"` 一個值，放進快照就等於斷言完全公開，但難度是模型填的 —— 察看告示被判 normal、一次失敗 `findQuestBoardView` 回 null，公告欄上的字卻沒有因此消失（T02 只拿到一份委託、T05 判 12 vs 12）。現在 `observe`／`interact` 打在資訊源上一律 `automatic`，但**不蓋掉**作者線索門檻／狀態檢定／`impossible`。② **氣氛不加社交難度**：`gateContextMods` 原本只濾 `ally_pressure`，`obscured`／`unstable_ground` 照樣套在社交上 —— 正是 T03「酒館裡的煙霧和人聲混合成一張無形的幕」總值 5 vs 12。現已剔除；`predicted` 刻意保留（那是洞察勝出的對抗結果，不是環境雜訊）。`publicInfoAndAmbience.test.ts` 7 條，停用規則即 3 紅。修後實測結果待回報 |
| B005 | 高 | 連續追問丟失對象或目的，反覆澄清 | 承接上一位守衛與問鎮長位置的目的，只問真正缺少的部分 | T05–T09；再補「剛剛那個守衛」仍應保留問路目的 | 已新增澄清標記，保留待釐清的原始問題與補答；並把近期問答送到回合後導演；自動測試 PASS，修後實測結果待回報 |
| B006 | 高 | 點地圖移動未留下玩家行動紀錄，抵達礦坑顯得突兀 | 對話明示地圖移動的目的地與結果；失敗亦有紀錄 | Harvey 更正：礦坑移動來自點地圖；不是已證明文字被誤路由 | 已保留地圖 action entry，接通過程／終局匯出；測試驗對話與匯出都有地圖行動；當場缺漏原因仍待複驗 |
| B007 | 高 | DM 提供未有設定依據的鎮長住址並稱可以前往 | 不捏造可導航地址或已存在的互動地點；裝飾性敘述須符合世界觀 | T09「東邊／市民廣場／大樓」；第一章未定義鎮長可互動地點 | 已補敘事與裁定約束。**新增**：`narrationService.buildUserMessage` 現在把 `dmContext.availableDestinations` 當白名單餵給敘事模型 —— 先前規則 14 要求模型遵守一份**它看不到**的清單（prompt 裡完全沒有任何目的地資訊，已用測試印出原始 prompt 證實）；空清單也明講「本場景目前沒有可前往的地點」而非靜默省略。`narrationService.test.ts` 3 條，移除該行即轉紅。⚠️ **仍是 prompt 層、不是後端保證** —— `resolveSceneTravel` 與 `resolveTargetPresence` 都不讀敘事散文，散文守門仍未實作。是否新增住處須另討論 |
| B008 | 高 | 朋友搭訕老闆娘的創意被 DM 改寫掉 | 保留合理行動的對象、方法、語氣、目的；NPC 自主回應，嘗試不保證成功 | 使用者補充；原句、改寫句及場次尚缺 | 已收窄 adjust 與敘事改寫規則。**根因已修（在引擎，不在提示詞）**：`turnService` 原本用 `effectivePlayerInput = adjustedIntent ?? playerInput` 把兩個字串**壓成一個**，所以只要 verdict 是 adjust，說書人就再也看不到玩家原話 —— 而 instructions 規則 2／15 要它保留的正是那些（搭訕的語氣、潑沙子的手法、推酒錢的動作）。現在兩者分開送：原話當風格素材，`adjustedIntent` 另立「結果上限」一行只約束成立到哪裡。⚠️ **世界痕跡刻意不跟著改** —— 痕跡會持久化並回饋進之後的提示，過度主張寫進去會變成偽正史；一次性敘事與持久世界狀態的防線不同級。⚠️ **反轉了一條既有測試**（`turnService.test.ts` 原本斷言 `not.toContain` 原話），理由記在該測試上方：擋過度主張的確定性防線是事實清單的 `[反向約束]`，不是把原話藏起來。需原句對照與真模型複驗 |
| B001 | 高 | 稍微探索就進入「遊蕩結局」 | 探索、問路與普通交談不等於拒絕劇情；先提供可理解的鉤子 | 使用者回報；缺終局原文、事件及觸發 ending ID | 已補回合後導演的近期問答，並允許 closing 在接回目標時 recovered。**收束入口已查明是兩條互不相連的路徑**：(A) 作者出口 `scenes.ts:243` 的 `idle_turns_at_least 20` + `read_mine_notice`；(B) 動態收束 —— `narrativeRecovery` 由模型提議 `conclude`，零引擎判準。**新增**：`resolveIdleTurns` 改為看得見本回合事件 —— 原判準只有 `hasActiveQuest`（等於「還沒撕告示」就一律 +1，問路與聊出線索全算閒晃），現在 `clue_revealed`／`quest_accepted`／`companion_joined` 會歸零（符合 idleTurns 原本的**連續**語意）。刻意不收 `affinity_changed`／`object_damaged`：朝老闆娘放火矢不算投入劇情（收進去會讓破壞變免死金牌，也會讓既有那條「idleTurns 19 的施法回合觸發出口」斷言失效）。**未做**：`value:20` 仍是 inline literal；`conclude` 仍可在第一個尾聲回合落地；兩個入口都無觀測 |
| B009 | 中 | 必要線索備援路徑覆蓋待檢查（設計改善項） | 必要結論有多個獨立取得途徑，單一檢定失敗不永久卡關 | 使用者提出 Three Clue Rule；尚無本場卡死結論的充分證據 | **盤點完成**，見 [B009 第一章線索備援盤點](參考資料/B009-第一章線索備援盤點.md)。一句話：**門是備援的，脊椎不是**。硬違規：通往第二章只有 1 條路（`entity_downed obj_worm_mother` 純由傷害計算，任何線索／談判／儀式都無法滿足；而宣傳的非戰鬥解法所設的 `reasoned_with_her` 全劇本沒有任何地方讀取）。**新增引擎稽核**：`findSingleSourceProgressionFlags`（integrity.ts）列出被自動出口／旅行選項／結局選項讀到、卻只有一個寫入點的旗標，真實劇本 21 筆；已排除兩類偽陽性（`flags_set_at_least` 的「以上任一」、委託 acceptFlag）。刻意**不進** DoD 門檻，理由同 `findScenarioWarnings`。補備援是寫劇本、零引擎改動 —— 待你決定 |
| B010 | 高 | 痕跡清單滿了後，新世界變化沒有存回去 | 滿額淘汰舊痕跡時，仍保存最新內容 | 本機新增測試先 FAIL；原程式只比陣列長度 | 已修正比較內容；回歸 PASS，非 RUN-001 已確認根因 |
| B011 | 中 | 本機 `/try` 故事匯出按鈕沒有接線 | 可下載／複製過程與結局，包含地圖操作、澄清、檢定與錯誤 | 本機 handler 原為 `pendingJourneyExport` | 已接線並可在冒險途中匯出；單元測試與路由 smoke PASS，真人下載／剪貼簿待回報 |

| B012 | 高 | 動態收束可以在第一個尾聲回合就鎖死結局 | 進入尾聲後至少給玩家 N 個回合接回目標 | `narrativeRecovery.advanceClosing` 算完 `closingTurns = state.closingTurns + 1` 就接受 `conclude`；乾淨狀態到鎖定結局最短只要 4 個合法回合（start_recovery → recovery_failed ×2 → conclude） | **未修**。前端 `guestStorage.ts:544` 的 `closingTurns >= 1` 是恆真的假約束。修法：`advanceClosing` 要求 `closingTurns >= N` 才接受 `conclude` |
| B013 | 高 | 兩個收束入口都沒有觀測，事後查不出為什麼結束 | 接受 `start_recovery`／`conclude` 與跨過閒晃門檻時要留下伺服器紀錄 | 動態路徑只在**駁回**提議時才寫 log（`narrativeAftermath.ts:116`）；`idleTurns` 遞增與 cold_feet 出口完全無 log | **未修**。這正是 B001 進度欄寫「需確認實際收束入口」的原因 —— 不是沒查，是查不到。修法：在接受轉移處與跨門檻處各加一行 log |
| B014 | 高 | 澄清沒有引擎狀態，重複追問沒有上限 | 同一個澄清問題不得連問兩次；未回答的澄清要明確帶進下一次裁定 | RUN-001 T05–T09 連問四輪。`wasClarification` 只標記「這輪是澄清」，沒有記錄**問的是哪一句**、也沒有「尚未回答的澄清」狀態 | **未修**（`wasClarification` 是第一步）。修法：verdict 為 clarify 時持存 `{playerInput, clarifyQuestion}`，注入 DM 訊息當「尚未回答的澄清」，新提議的 `clarifyQuestion` 正規化後與上一次相同就駁回，重試一次後改當行動處理 |
| B015 | 中 | 世界導演只看單一回合，分不出「追問澄清」與「拒絕劇情」 | 判斷玩家是否拒絕劇情時要看得到近期對話 | `buildWorldUpdatePrompt` 只傳在場 id、既有痕跡、本回合事實、一行意圖與 recovery 狀態 —— 沒有 `recentTurns`、沒有場景描述 | **未修**。新加的「不能把追問澄清當拒絕機會」規則要求模型做一個**它沒有資料能做**的區分，與 B007 缺白名單同一種 prompt-vs-engine 落差 |
| B016 | 中 | 世界痕跡的自由文字未經驗證，會變成偽正史 | 痕跡文字不得引入未設定的地點或狀態 | `mutations[].text` 與 `narrative.opportunityText` 是自由散文，會存進 progress 並在之後的回合回饋給模型（`worldUpdateService.ts:306-313`）；`gateNpcDepartures` 與 `applySceneMutations` 都只驗 id、不驗文字 | **未修**。這是獨立於敘事串流的第二條幻覺洗白路徑：憑空的地址一旦寫進痕跡，之後的回合會當成既成事實 |

## 手動驗收清單

只有 Harvey 複驗認可後才勾選；勾選時在該項補上 RUN 編號／日期，日後復發取消勾選並保留舊證據。

- [ ] B003 查看告示不會因術式預選而施法
- [ ] B002 公告欄與同義描述能取得正確完整委託
- [ ] B004 日常行動不被強迫擲骰
- [ ] B005 連續對話與澄清答覆能承接完整意圖
- [ ] B006 地圖操作、目的地與結果完整出現在對話
- [ ] B007 不再捏造可前往的鎮長住處
- [ ] B008 合理創意行動保留原意
- [ ] B001 普通探索不過早收束成遊蕩結局
- [ ] B009 必要結論的線索備援已盤點並驗收
- [ ] B012 進入尾聲後仍有回合可以接回目標
- [ ] B013 收束原因可以從伺服器紀錄查出來
- [ ] B014 同一個澄清問題不會連問兩次
- [ ] B015 追問澄清不會被判成拒絕劇情
- [ ] B016 世界痕跡不會寫進未設定的地點
- [ ] B010 痕跡滿額後新變化仍能保存
- [ ] B011 單人故事下載與複製能完整帶走當次開頁紀錄

## 新增 bug 格式

下一筆從 B012 開始；表格新增一列，下方新增同 ID 的 checkbox。記下「玩家原句／操作 → 實際結果 → 預期結果」、RUN 與段落、當場版本（若知道）。未重現與已修正分開記；不要為缺少證據補造事件。

## 待討論事項

- **B009（盤點後新增）**：① `obj_worm_mother` 要不要開非戰鬥通章路徑（`reasoned_with_her` 目前沒人讀）？
  ② `quest_missing_miners.completionFlag` 是 `null` 是刻意的嗎 —— 它讓主線與支線互斥（`canAcceptQuest` 會擋掉所有支線）？
  ③ `harold_joined` 一次 hard 定生死、且是第四章五個 `ALLY_FLAGS` 之一，要不要補第二寫入點或補救路徑？
  ④ 第一章 21 個檢定線索有 13 個沒有 `designedDifficulty`（DC 由模型每次重猜），要不要批次補？
- **B007 下一步**：散文守門只能做成 detect-and-log／append，不能改寫 —— SSE 開始後無法回捲。
  可掛的位置是 `narrationAudit`（既有兩類 pattern）＋ `turnService` 串流結束後那個唯一的 hook。

- B001：取得真正終局與事件後，區分作者出口與動態收束；探索容許時間和結局節奏再依體驗調整。
- B007：第一章是否要新增鎮長住處與 NPC，屬劇本擴充；目前先要求 DM 如實說明未知，不自行新增地圖。
- B008：補朋友的原句與改寫句，才能判斷是哪一層剝掉創意。
- B006：已確認 `/try`；本機有地圖 action entry，但當場缺漏仍未重現。本機匯出原為佔位函式，本輪已接通；仍不能用本機狀態倒推當場匯出方式或版本。


## 第三次真人實測追加（2026-09-08）

| 編號 | 優先 | 問題 | 預期 | 證據 | 狀態 |
|---|---|---|---|---|---|
| B017 | 高 | 故事匯出無敵我檢定標籤與實際傷害，容易把檢定總值當傷害 | 每筆顯示行動者、對象、命中部位與實際傷害 | [RUN-003](場次/RUN-003-廢棄磨坊/結果與觀察.md)；turnJournal.buildDmResponse 只列 check_resolved 總值 | 已修：敵我檢定、受傷對象、部位與傷害逐筆顯示／匯出，名稱隨日誌保存；自動測試通過，待 Harvey 真人複驗 |

- [ ] B017 匯出可辨認敵我檢定、實際傷害與命中部位
- B008 新證據：RUN-003 指定嘴部射擊但落在軀幹。普通成功在 resolveImpactPart 會強制改成軀幹；尚未拿到本場原始提案，待討論規則，不宣稱只改 AI 就能修好。
