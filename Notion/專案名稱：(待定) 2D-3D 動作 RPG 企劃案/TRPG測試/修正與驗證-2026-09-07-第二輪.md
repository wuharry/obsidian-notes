# 2026-09-07 第二輪修正與驗證

[測試索引](README.md)｜[bug 與手動驗收](BUGS.md)｜[首輪歷史結果](修正與驗證-2026-09-07.md)

> Harvey 明確授權直接修正、commit 與 push。本輪程式 commit：`ai-wrapper d16259e`。
> Harvey 正在做真模型與真人實測；修正後結果待回報。Codex 的自動測試不能替代這項驗收，所有 checkbox 保留未勾。

## 已完成的程式變更

| 問題 | 改動 | 驗證 |
|---|---|---|
| B004 日常亂擲骰 | 模型先提供 `checkReason.uncertainty` 與 `failureConsequence`；即時輸出缺理由就正規化成 null，普通行動不擲骰。攻擊、作者線索及需要檢定的狀態仍照原規則 | 六個引擎案例；另驗真實服務入口漏填理由時不產生骰子事件 |
| B005 澄清丟失目的 | `/try` 用 `wasClarification` 標記澄清回合；context 明列原始問題與已補答內容，不從關鍵字猜對象。歷史長文字限制在 HTTP 可接受範圍 | context 合併、HTTP 相容／非法值、前端保留澄清與長度測試 |
| B001 導演缺前文／收束後無法回頭 | 回合後世界／敘事模型也收到近期對話。`closing` 尚未結局時允許 `recovered`；未完成回合不能提交恢復、已結局不能改寫 | 服務資料流、敘事狀態機回歸 |
| B006／B011 地圖紀錄與匯出 | 單人頁接通故事下載／複製，途中亦可匯出；完整保留地圖行動、場景敘述、澄清、拒絕、檢定與錯誤，DM 進場不偽裝成玩家行動 | 同一份 journal 驗畫面、匯出順序、字數上限及按鈕停用；路由 smoke |
| B010 世界變化未保存 | 原本只比較痕跡陣列長度，滿額後新內容會被當成沒變。改比每筆內容，重複提議仍不發多餘更新 | 新測試先 FAIL，再修正至 PASS |

## 契約與保存影響

| 欄位／入口 | Producer → consumer | 相容性／權限 |
|---|---|---|
| `checkReason` | DM structured output → `requestDmVerdict` → `gateActionProposal` | 新模型 strict 格式要求填值或 null；既有離線提案 undefined 保留舊重播規則。沒有改 DC、傷害或資料庫 schema |
| `wasClarification` | `/try` journal → HTTP `recentTurns` → `formatRecentTurns` → 裁定／敘事／世界更新 | 選填，舊客戶端可省略；僅供語意承接，不得覆蓋 engine facts。多人 DB history 本輪未新增持久欄位 |
| 回合後近期問答 | `runTurn`／文字旅行 → `requestTurnAftermath` → `requestWorldUpdate` | 導演可以理解短句補答；增加上下文，不額外新增模型呼叫 |
| 故事檔案 | `/try` 現有 entries → Markdown → 瀏覽器下載／剪貼簿 | 包含目前開頁期間全部紀錄，不等於跨重整永久日誌；匯出正文註明範圍，使用者下載後自行保存 |

## 實際驗證

| 命令／檢查 | 結果 |
|---|---|
| server `bun test` | PASS：1520 pass／0 fail，98 個測試檔 |
| web `bun test` | PASS：135 pass／0 fail，26 個測試檔 |
| web `bun run test:smoke` | PASS：9 條路由；此檢查驗 SSR／HTTP，不驗真人點擊、剪貼簿或模型品質 |
| server／web `bun run check-types` | PASS |
| server／web `bun run lint` | PASS |
| `python3 .harness/verify.py` | PASS 投遞完整性；不能替代應用驗收 |
| `git diff --check` | PASS |
| commit hook | 全工作區 typecheck、Prettier、規則同步檢查通過；格式化結果已包含在 commit |
| Harvey 修後實測 | 進行中／結果待回報；本輪未代跑真模型 |

首輪兩個 `worldUpdateService.test.ts` 失敗的原因已查清：測試要記錄蛛網的痕跡，fixture 卻漏列真實磨坊中存在的 `obj_spider_webs`。本輪補正 fixture，保留原期待值與「不存在 target 必須拒絕」測試；沒有鬆綁引擎閘門。

## 回家接續

1. 同步 `ai-wrapper` 與 `obsidian-notes`，確認程式包含 `d16259e`，先讀當地 AGENTS 與本頁。
2. 在 `/try` 依 RUN-001 複驗：魔法面板保留預選 → 查看告示 → 普通搭話 → 向守衛連續問路 → 點地圖 → 下載／複製。
3. 新增 RUN-002，保存新故事流程與終局原文。回報原句、操作與實際結果；不要改寫 RUN-001 或自動勾 checkbox。
4. B001 仍需真正的終局／事件辨識觸發入口；`idleTurns >= 20` 作者出口沒有調整。B007 的地址、B008 的創意保留仍需真模型對照，提示詞不能保證零幻覺。B009 線索備援盤點尚未完成。
5. 後續涉及自由度／節奏取捨，拿具體案例與 Harvey 對齊；答案在程式裡就先查，不反覆索取已授權的 commit／push 確認。
